mixin DashboardStyle {

  calculateWidth(screenWidth) => screenWidth * .96;
  calculateHeightDashboard(screenHeight) => screenHeight * .70;
  calculateHeightMenu(screenHeight) => screenHeight * .11;
  calculatePaddingTopMenu(screenHeight) => screenHeight * 0.03;
}
